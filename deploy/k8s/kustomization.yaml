apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  # secret.yaml is gitignored and applied by hand — see secret.example.yaml
  - migrate-job.yaml
  - embeddings.yaml
  - app.yaml
  - worker-heavy.yaml

# notes-gc (CronJob) is M8 — nothing to garbage-collect meaningfully before
# soft-delete (M6) exists. See docs/architecture/08-roadmap.md.
