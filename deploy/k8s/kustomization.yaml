apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  # secret.yaml is gitignored and applied by hand — see secret.example.yaml
  - migrate-job.yaml

# Deployments (bot, worker-fast, worker-heavy, embeddings, gc) land with M2
# onward, once each process does something real — see
# docs/architecture/08-roadmap.md.
