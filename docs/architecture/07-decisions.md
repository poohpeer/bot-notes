# 07. Решения, отклонения и риски

## Архитектурные решения

### ADR-1. Монорепо, три образа, пять единиц развёртывания

Альтернатива - один образ на всё. Отвергнута: под бота получил бы torch,
ffmpeg и веса Whisper, то есть полтора гигабайта неиспользуемых зависимостей
и долгий холодный старт. Альтернатива «отдельный репозиторий на сервис»
отвергнута тоже: у бота и воркеров общие модели данных, ACL и чанкинг, и
синхронизировать их между репозиториями дороже, чем собрать два таргета из
одного Dockerfile.

### ADR-2. Embedding-модель за HTTP-сервисом с ролью текста

Клиенты запрашивают вектор для текста «в роли запроса» или «в роли пассажа».
Префиксы e5, нормализация, токенизация и батчинг скрыты внутри сервиса.

Следствие: открытый вопрос дизайн-дока о выборе между
`paraphrase-multilingual-mpnet-base-v2` и `intfloat/multilingual-e5-base` не
блокирует разработку - он меняет только содержимое одного сервиса и требует
пересчёта векторов, но не правок в боте, воркерах или SQL.

### ADR-3. Размерность 768 фиксирована, модель версионируется в данных

Обе модели-кандидата дают 768 измерений (почти точно), поэтому `VECTOR(768)`
покрывает обе. Колонка `note_chunks.embedding_model` хранит, какой моделью
посчитан каждый вектор, что делает возможной поэтапную миграцию без простоя.
Размерность вынесена в конфиг и сверяется со сервисом на старте.

### ADR-4. Двухфазный поиск с дедупликацией в подзапросе

Запрос из дизайн-дока сортирует результат по `n.id`, а не по релевантности -
подробный разбор в `04-search.md`. Целевая схема: ANN top-K по чанкам,
затем `DISTINCT ON` внутри подзапроса, затем `ORDER BY distance` и страница
снаружи.

### ADR-5. Приватность fail-closed

Дизайн-док описывает вопрос о приватности кнопками при сохранении. Буквальная
реализация - «ждать нажатия, потом сохранять» - создаёт состояние, в котором
заметка не сохранена и может потеряться, если пользователь отвлёкся.

Решение: заметка сохраняется сразу с `visibility` из
`user_settings.default_visibility` (дефолт `'private'`), а кнопки работают
как переключатель уже проставленного значения. Не нажал - осталась
приватной. Ошибка в безопасную сторону, обработка при этом идёт параллельно с
раздумьем пользователя.

Статус: **подтверждено владельцем продукта.**

### ADR-6. Три очереди с раздельными воркерами

`fast` - текст и страницы, секунды. `heavy` - скачивание медиа и Whisper,
минуты. `llm` - обогащение через proxy-ai, не срочно. Общая очередь означала
бы, что сохранение текстовой заметки ждёт окончания транскрипции
двадцатиминутного видео.

### ADR-7. SSRF-защита обязательна, не опциональна

Пользователь диктует URL, бот живёт внутри кластера с доступом к Postgres,
Redis и внутренним сервисам. Без проверки адресов бот становится прокси для
разведки внутренней сети. Требования перечислены в `03-ingest.md`; эта
защита входит в объём первого же этапа, где появляется загрузка страниц, а не
откладывается на «потом».

### ADR-8. Идемпотентность приёма по `(chat_id, tg_message_id)`

Telegram повторяет доставку при таймаутах, бот перезапускается при деплое.
Уникальный индекс плюс `ON CONFLICT DO NOTHING` делают повторную обработку
бесплатной. Задачи RQ ставятся с детерминированным `job_id`.

### ADR-9. ai-proxy за портом, с рабочей заглушкой

`LLMClient` как протокол, три реализации, флаг `LLM_ENABLED`. `NullLLMClient`
позволяет довести систему до рабочего состояния без заголовков и тегов
независимо от готовности интеграции с ai-proxy.

Контракт ai-proxy теперь зафиксирован (провайдер `codex`, README на `e648574`,
подробности в `05-contracts.md`). Ключевые следствия для реализации:

- `model` не передаётся - codex всегда использует дефолтную модель аккаунта;
- структурированный вывод по схеме codex не даёт, поэтому `output_format="text"`
  и разбор JSON выполняет `ProxyAILLMClient` сам;
- `503 quota_exhausted` (все аккаунты исчерпали квоту) обрабатывается отдельно
  от прочих ошибок и не ретраится вслепую;
- `timeout_s` задаётся явно под задачу, дефолт CLI 300 с слишком велик для чата.

### ADR-14. codex в ai-proxy без песочницы - блокер для LLM_ENABLED=true

ai-proxy запускает codex как `codex exec --json
--dangerously-bypass-approvals-and-sandbox`: модель выполняет любые
shell-команды без песочницы. Это приемлемо для собственных задач ai-proxy в
изолированном контейнере, но небезопасно, когда в промпт попадает произвольный
текст заметки, где возможна инъекция инструкций (см. риск R5).

Решение: обогащение и `/smart_search` инструментов не используют (`mcp_url`
не передаётся), поэтому в ai-proxy для таких запросов codex должен запускаться
в песочнице только для чтения. Фикс - в репозитории `poohpeer/ai-proxy`
(`ai-proxy#15`), смержен и задеплоен. `LLM_ENABLED=false` в конфиге бота
остаётся дефолтом и после фикса - включение LLM теперь осознанное
продуктовое решение (стоимость, нагрузка), а не вопрос безопасности.

Статус: **подтверждено владельцем продукта, вариант «фикс в ai-proxy».**

### ADR-10. В группах сохраняется только адресованное боту

Дизайн-док говорит, что все сообщения в группе автоматически сохраняются.
Буквально это означает, что вместе с заметками в базу и в векторный индекс
попадёт вся болтовня: «ок», «сейчас приеду». Это ухудшит поиск и потратит CPU
на эмбеддинг мусора.

Решение: `chat_settings.capture_mode` с двумя режимами.

| Режим | Что сохраняется |
|---|---|
| `mentions_and_replies` (по умолчанию) | Только сообщения с упоминанием бота через `@` и ответы на сообщения бота. Команды бота обрабатываются как команды |
| `all` | Всё, как в дизайн-доке |

Статус: **подтверждено владельцем продукта, выбран `mentions_and_replies`.**

У этого решения есть важное побочное следствие. Дизайн-док требовал выключить
privacy mode бота у BotFather, чтобы бот видел все сообщения группы. При
режиме `mentions_and_replies` предполагалось, что это не нужно.

**Проверено на живой группе (2026-09-18) - предположение не подтвердилось.**
С **включённым** privacy mode Telegram доставляет боту команды и ответы на
его сообщения, но **не** доставляет обычные сообщения с `@`-упоминанием
бота - апдейт до бота вообще не доходит (ни "handled", ни "not handled" в
логах диспетчера), хотя `mentions_bot()` реализована верно и
`getWebhookInfo` подтверждал пустую очередь без вебхука (то есть апдейт не
завис - Telegram его просто не поставил в очередь боту). После выключения
privacy mode у BotFather (`Bot Settings → Group Privacy → Disable`)
меншены заработали немедленно. Вывод: для режима `mentions_and_replies`
privacy mode тоже нужно выключать, вопреки исходному предположению этого
ADR - иначе `@`-меншены в группе тихо теряются на стороне Telegram, до
всякой логики бота.

Что это даёт (уже без предположения о privacy mode):

- бот всё ещё не получает переписку участников группы, не адресованную
  ему, - меньше данных, меньше ответственности за них, меньше поверхность
  утечки, хотя доставка технически включена;
- меньше апдевов, требующих реальной обработки (болтовня без меншена/реплая
  всё равно отбрасывается `should_capture_group_message`), хотя сами
  апдейты теперь доходят до бота.

Режим `all` остаётся доступным командой в группе, но требует ручного
выключения privacy mode у BotFather - без этого бот просто не увидит
остальные сообщения и режим не заработает. Это ограничение платформы, а не
нашей реализации; бот при переключении в `all` должен об этом предупредить.

Цена решения: чтобы сохранить ссылку в группу, надо упомянуть бота или
ответить на его сообщение, а не просто кинуть ссылку. Это на одно действие
больше, зато групповой индекс содержит только то, что участники сознательно
сохраняли.

### ADR-11. Long polling, одна реплика бота

Webhook требует Ingress, публичного DNS и TLS-сертификата. Для одного бота с
одним пользовательским потоком это лишняя инфраструктура. Цена решения -
короткий даунтайм при деплое и невозможность горизонтального
масштабирования бота. Развилка зафиксирована; переход на webhook затронет
только слой транспорта в `notes-bot`.

### ADR-12. ACL как тестируемый модуль, а не строки SQL

Три предиката видимости живут в `domain/acl.py` и покрываются табличными
тестами по матрице `(автор, чат, visibility, is_group, режим поиска)`. Это
единственное место, ошибка в котором означает утечку приватных заметок между
пользователями, поэтому оно отделено от остального SQL и тестируется
отдельно.

### ADR-13. Голосовые заметки от пользователя

Дизайн-док упоминает Whisper только для аудио из Instagram. Но инфраструктура
транскрибации - образ `notes-app-heavy`, очередь `heavy`, отдельный воркер -
всё равно будет построена, и голосовое сообщение боту это самый естественный
способ быстро сохранить мысль на ходу.

Решение: `source_type='voice'`. Голосовое сообщение скачивается через Bot
API, транскрибируется тем же `faster-whisper`, транскрипт индексируется.
Подробности в `03-ingest.md`.

Статус: **подтверждено владельцем продукта.**

Влияние на систему:

- дополнительный поток задач в очереди `heavy`, но короткими задачами -
  голосовые обычно секунды или минуты против десятков минут у видео;
- оценку ресурсов `notes-worker-heavy` придётся пересмотреть по факту, если
  голосовые окажутся частым способом ввода;
- редактирование на `voice` не распространяется, потому что дизайн-док
  разрешает править только `source_type='text'`; правило можно будет
  расширить позже без изменений в схеме.

## Отклонения от дизайн-дока

Все отклонения подтверждены владельцем продукта; вопросов, блокирующих старт
имплементации, не осталось.

| # | Дизайн-док | Принято | Почему |
|---|---|---|---|
| 1 | `SELECT DISTINCT ON ... ORDER BY n.id, distance LIMIT 6 OFFSET` | Дедупликация в подзапросе, сортировка по расстоянию снаружи | Исходный запрос возвращает заметки с наименьшими id, а не самые релевантные |
| 2 | В Redis хранится `query_vector` | В Redis хранится ранжированный список `note_ids` | Повторный ANN на каждую страницу может дать другой порядок - дубли и пропуски между страницами |
| 3 | Бот спрашивает приватность при сохранении | Сохраняем сразу с `private`, кнопки переключают | Нет состояния «не сохранено, ждём кнопку»; ошибка в безопасную сторону |
| 4 | Все сообщения в группе сохраняются | Только упоминания бота и ответы на его сообщения; режим `all` включается командой | Болтовня в векторном индексе портит поиск. Побочный эффект: privacy mode у BotFather можно не выключать |
| 5 | Схема без `title`, `summary`, полей для мест | Добавлены `title`, `summary`, `structured JSONB` | Дизайн-док описывает эти LLM-фичи, но полей под них не предусматривает |
| 6 | `raw_text` одно поле | `raw_text` + `extracted_text` | Редактирование разрешено только для пользовательского текста; их нужно различать |
| 7 | Индекса на `embedding` нет | HNSW `vector_cosine_ops` | Без индекса поиск линеен по всем чанкам |
| 8 | - | `tg_message_id` + уникальный индекс, `attempts`, `error`, `enrich_status` | Идемпотентность и разбор сбоев |
| 9 | Whisper только для Instagram | Добавлен `source_type='voice'` - голосовые заметки от пользователя | Инфраструктура транскрибации всё равно строится; ADR-13 |

Пункты 1 и 2 - исправления ошибок в дизайн-доке. Пункты 3, 4 и 9 - изменения
поведения, видимые пользователю; подтверждены отдельно. Остальные - дополнения
схемы под фичи, которые дизайн-док описывает, но не покрывает полями.

## Риски

| # | Риск | Вероятность | Влияние | Что делаем |
|---|---|---|---|---|
| R1 | YouTube и Instagram блокируют запросы с адресов дата-центра | Высокая | Ломаются два из пяти типов источников | Заметка деградирует до ссылки, а не падает; метрика ошибок по источникам с алертом; при подтверждении - куки или исходящий прокси с резидентным адресом |
| R2 | Whisper на CPU медленнее приемлемого | Средняя | Очередь `heavy` растёт, заметки обрабатываются часами | Замер на первой же неделе; запасные ходы: модель `base`, лимит длительности 10 минут, отключение транскрипции для длинных постов |
| R3 | SSRF через пользовательские URL | Высокая при отсутствии защиты | Разведка внутренней сети кластера | Обязательная защита с первого этапа, ADR-7 |
| R4 | Смена embedding-модели после накопления данных | Средняя | Полный пересчёт векторов | `embedding_model` в каждой строке, поэтапная миграция, выбор модели закрывается на M1 до накопления данных |
| R5 | Инъекция промпта из содержимого заметки | Устранена (была высокая) | codex в ai-proxy выполнял shell-команды без песочницы - инъекция вела к RCE в контейнере ai-proxy | ADR-14: фикс в ai-proxy (`ai-proxy#15`, read-only песочница для запросов без `mcp_url`) - смержен и задеплоен, `LLM_ENABLED=true` больше не заблокирован. Дополнительно: текст заметки как данные в отдельной секции промпта; вывод LLM валидируется по схеме, не исполняется и не влияет на ACL |
| R6 | `yt-dlp` ломается при изменениях на площадках | Высокая | Постепенная деградация извлечения | Не пинить версию жёстко, регулярная пересборка `notes-app-heavy`, метрика ошибок |
| R7 | Общий Postgres с family-bot | Низкая | Взаимное влияние по ресурсам | Отдельная роль и права, удаление пачками в GC, без длинных транзакций |
| R8 | Шум в групповом индексе | Высокая при режиме `all` | Деградация качества поиска | ADR-10; в режиме по умолчанию риск снят |
| R11 | Участники группы не понимают, что надо упомянуть бота | Средняя | Заметки не сохраняются, а люди думают, что сохранились | Приветственное сообщение при добавлении бота в группу; периодическое напоминание не нужно, достаточно понятного ответа бота на каждое сохранение |
| R9 | Даунтайм бота при деплое | Точно будет | Секунды недоступности | Принято осознанно, ADR-11 |
| R10 | Пост-фильтрация ACL поверх HNSW даёт неполную выдачу | Средняя при росте объёма | Часть релевантных заметок не находится | На старте объём мал и работает точный скан; меры подготовлены в `04-search.md` |

## Открытые вопросы

Из дизайн-дока, со статусом после проектирования:

| Вопрос | Статус | Когда закрывается |
|---|---|---|
| Контракт ai-proxy | **Закрыт.** Провайдер `codex`, контракт зафиксирован в `05-contracts.md` | - |
| Выбор embedding-модели | Не блокирует. Изолирован сервисом. Обе кандидатуры дают 768 измерений | M1 - бенчмарк на реальных ru/en/he заметках |
| Выбор LLM-модели в ai-proxy | **Закрыт.** codex использует дефолтную модель ChatGPT-аккаунта, `model` не передаётся | - |
| Фикс песочницы codex в ai-proxy | Открыт. Блокер `LLM_ENABLED=true`, задача в репозитории `ai-proxy` | До включения LLM на M5 |

Возникшие при проектировании и уже закрытые:

| Вопрос | Решение |
|---|---|
| Сохранять в группах всё или только адресованное боту | Только упоминания и ответы боту, ADR-10 |
| Приватность до нажатия кнопки | Сохраняем сразу как `private`, ADR-5 |
| Нужны ли голосовые заметки | Да, `source_type='voice'`, ADR-13 |

Остаётся открытым:

1. **Версия pgvector на существующем инстансе.** От неё зависит доступность
   итеративного скана индекса, мера №2 из `04-search.md`. Проверяется одним
   запросом к базе, не блокирует старт.
2. **Поведение privacy mode на живой группе.** ADR-10 исходит из того, что при
   включённом privacy mode бот получает упоминания и ответы на свои
   сообщения. Это почти точно так, но проверить надо на реальной группе при
   реализации M6 - если окажется иначе, privacy mode придётся выключать, как
   и предполагал дизайн-док.
